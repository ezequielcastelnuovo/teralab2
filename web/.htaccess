RewriteEngine On

# Excepción total para health (sin redirect, sin front-controller)
RewriteRule ^health(\.php|\.txt)?$ - [L]

# Front controller típico
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.php [L]
